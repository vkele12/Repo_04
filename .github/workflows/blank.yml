name: CI build

on:
 push:
    branches: [ "Feature" ]
  

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: display message
      run: echo " Hi Vishwanath, Hello 123 ,abcd"
